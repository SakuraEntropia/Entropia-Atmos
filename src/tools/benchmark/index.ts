/** Benchmark tooling — quantitative pipeline comparison (SPEC M-05). */
export type { DirMetrics, RenderMetrics, BenchReport, BenchComparison } from "./benchmark";
export { measureDir, measureRender, energyDb, compareBenchmarks } from "./benchmark";
