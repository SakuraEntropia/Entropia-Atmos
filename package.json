{
  "name": "entro-atmos",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "ENTRO ATMOS — AI + Graphics inspired spatial audio engine (foundation: documentation + module contracts).",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
