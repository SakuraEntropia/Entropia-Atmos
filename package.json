{
  "name": "entro-atmos",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "ENTRO ATMOS — AI + Graphics inspired spatial audio engine (AudioGS, Audio-USD, acoustic simulation, HRTF binaural rendering).",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "render": "tsx src/tools/cli/render.ts",
    "audiogs": "tsx src/tools/cli/audiogs.ts",
    "bench": "tsx src/tools/cli/bench.ts"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.5.4",
    "vitest": "^2.1.9"
  }
}
